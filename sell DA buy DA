--[[
================================================================================
          🤖 DELTA EXECUTOR SPECIAL: ALL-IN-ONE HYBRID SYSTEM 🤖
================================================================================
- Tối ưu hóa 100% cấu trúc HTTP Request cho Delta Mobile Executor.
- Gom chung logic Mua, Bán, Nhận và tự động phân vai vế theo Index.
]]

if not game:IsLoaded() then game.Loaded:Wait() end
task.wait(3)

-- 🔴 THAY LINK WEB APP APPS SCRIPT CỦA BẠN VÀO ĐÂY
local SHEETS_URL = "https://script.google.com/macros/s/AKfycbyFa5a5S5BFXMfpQeiJUmc9ANfUMlQPfEU-_xxwTvbO9vf-rs_UL9eYyvgfG9dCk59Q/exec"

local HttpService = game:GetService("HttpService")
local LP = game:GetService("Players").LocalPlayer

-- Cấu hình tương thích cho hàm Request của Delta
local delta_request = (syn and syn.request) or request or http_request or (http and http.request)

local MAX_COINS = 100000000 -- 100M chặn tối đa của Seller
_G.COINS_MAXED = false

local CONFIG = {
    UNDERCITY_ID = 4601778915,
    MARKET_REMOTE = game:GetService("ReplicatedStorage"):WaitForChild("Remotes"):WaitForChild("PlayerMarketRemote")
}

-- Chờ nhân vật tải xong
while not LP.Character or not LP.Character.Parent do LP.CharacterAdded:Wait() end
LP.Character:WaitForChild("HumanoidRootPart", 30)

print("[DELTA-SYSTEM] ⚙️ Khởi động thành công trên Delta cho: " .. LP.Name)

-- =================================================================================
-- HÀM GỌI API BẰNG PHƯƠNG THỨC GET ĐƯỢC ĐO MAY RIÊNG CHO DELTA
-- =================================================================================
local function apiCall(queryString)
    local success, res = pcall(function()
        return delta_request({
            Url = SHEETS_URL .. queryString,
            Method = "GET" -- Dùng GET trực tiếp qua URL để Delta xử lý mượt nhất
        })
    end)
    if success and res and res.Body then return res.Body end
    return nil
end

local function getQueue()
    local body = apiCall("?action=getQueue")
    if body then 
        local success, decoded = pcall(function() return HttpService:JSONDecode(body) end)
        if success then return decoded end
    end
    return nil
end

local function setCoin(user, coin)
    apiCall("?action=setCoin&user=" .. user .. "&coin=" .. tostring(coin))
end

-- ==========================================
-- LUỒNG ĐỒNG BỘ COIN REALTIME LÊN SHEET
-- ==========================================
task.spawn(function()
    while true do
        pcall(function()
            local dataFolder = LP:ClearCharacterAppearanceBeforeInstance() or LP:WaitForChild("Data", 10)
            local coinsObj = dataFolder:WaitForChild("Currency", 10):WaitForChild("Coins", 10)
            if coinsObj then
                setCoin(LP.Name, coinsObj.Value)
                if coinsObj.Value >= MAX_COINS then
                    _G.COINS_MAXED = true
                else
                    _G.COINS_MAXED = false
                end
            end
        end)
        task.wait(5)
    end
end)

-- =================================================================================
-- VÙNG CHỨA LOGIC SELLER (TỰ ĐỘNG TREO BÁN ĐỒ & NHẬN TIỀN VẬT LÝ)
-- =================================================================================
local function getInventoryItems()
    local items = {}
    pcall(function()
        local inv = LP:WaitForChild("Data", 10):WaitForChild("Inventory", 10)
        for _, item in ipairs(inv:GetChildren()) do
            if item:IsA("NumberValue") and item.Value > 0 then
                table.insert(items, { Name = item.Name, Amount = item.Value })
            end
        end
    end)
    return items
end

local function sellItem()
    pcall(function()
        local inventory = getInventoryItems()
        if #inventory == 0 then return end
        
        table.sort(inventory, function(a, b) return a.Amount > b.Amount end)
        local target = inventory[1]
        
        local currentPrice = 99999
        if target.Name:lower():find("apple") then currentPrice = 110 end

        local args = {
            [1] = "Publish",
            [2] = { ["Price"] = currentPrice, ["ItemName"] = target.Name, ["Amount"] = 1 }
        }
        CONFIG.MARKET_REMOTE:InvokeServer(unpack(args))
    end)
end

local function claimMarket()
    pcall(function()
        CONFIG.MARKET_REMOTE:InvokeServer(unpack({ [1] = "ClaimAll" }))
    end)
end

task.spawn(function()
    while true do
        if game.PlaceId == CONFIG.UNDERCITY_ID then
            pcall(function()
                claimMarket()
                task.wait(0.3)
                if not _G.COINS_MAXED then
                    for _ = 1, 5 do
                        sellItem()
                        task.wait(0.1)
                    end
                end
            end)
        end
        task.wait(2)
    end
end)

-- =================================================================================
-- VÙNG CHỨA LOGIC SOUR BUYER (TỰ CHẠY ĐẾN MUA COIN THEO INDEX TIẾN)
-- =================================================================================

-- Tích hợp hàm mua vật lý nguyên bản trong file SOUR.lua của bạn
local function runScript3(sellerName)
    print(string.format("[DELTA-GAME] 💸 Đang kích hoạt lệnh mua hàng từ shop của: %s", sellerName))
    
    -- Bạn chèn đoạn mã gọi Remote mua hàng gốc của file SOUR.lua vào đây:
    pcall(function()
        -- Ví dụ: game:GetService("ReplicatedStorage").Remotes.BuyItemRemote:InvokeServer(sellerName, ...)
    end)
end

-- LUỒNG QUẢN TRỊ ĐIỀU PHỐI CHUỖI TIỀN CỦA DELTA
task.spawn(function()
    print("[DELTA-CHAIN] 🚀 Bắt đầu quét chuỗi dồn tiền tịnh tiến...")
    
    while true do
        local queue = getQueue()
        
        if queue and #queue >= 2 then
            for i = 1, #queue - 1 do
                local accA = queue[i]     -- Acc liền trước (Cấp tiền)
                local accB = queue[i + 1] -- Acc liền sau (Nhận tiền)

                -- Nếu tên acc đang bật Delta trùng với AccB và phát hiện AccA có tiền mặt dư trên Sheet
                if LP.Name:lower() == accB.user:lower() and accA.coin > 0 then
                    
                    print(string.format("[DELTA-CHAIN] 🎯 Phát hiện số dư: %s đang có %d Coins.", accA.user, accA.coin))
                    print(string.format("[DELTA-CHAIN] 🛒 Thực hiện hành động: Tôi (%s) chạy đi mua từ quầy %s", LP.Name, accA.user))
                    
                    local transferValue = accA.coin
                    
                    -- Thực hiện thao tác mua vật lý
                    local successBuy, buyErr = pcall(function()
                        runScript3(accA.user)
                    end)
                    
                    if successBuy then
                        print("[DELTA-CHAIN] ✅ Mua thành công! Đang đồng bộ cập nhật lại số liệu về Sheet...")
                        
                        -- Bước 1: Reset ví của acc vừa mua về 0 ngay lập tức để khóa luồng
                        setCoin(accA.user, 0)
                        task.wait(0.5)
                        
                        -- Bước 2: Dồn số coin đó cộng trực tiếp vào ví của mình trên Sheet
                        local myNewBalance = accB.coin + transferValue
                        setCoin(LP.Name, myNewBalance)
                        
                        print(string.format("[DELTA-CHAIN] 🎉 Hoàn tất vòng dồn tiền! Số dư mới: %d Coins.", myNewBalance))
                    else
                        warn("[DELTA-CHAIN] ❌ Lỗi gọi hàm mua vật lý trên Delta: ", buyErr)
                    end
                    
                    task.wait(2)
                end
            end
        end
        task.wait(5) -- Định kỳ 5 giây rà soát bảng thông báo một lần
    end
end)

-- Auto Coin Farm - Chạy là nhặt coin tự động
local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer

local farmedCoins = {}  -- Lưu các coin đã nhặt để tránh nhặt lại

task.spawn(function()
    while task.wait(0.05) do
        local character = LocalPlayer.Character
        local rootPart = character and character:FindFirstChild("HumanoidRootPart")
        if not rootPart then continue end

        local closestCoin = nil
        local minDist = math.huge

        -- Tìm coin gần nhất
        for _, obj in ipairs(workspace:GetDescendants()) do
            if obj:IsA("BasePart") and string.find(obj.Name, "Coin") and not farmedCoins[obj] then
                local distance = (rootPart.Position - obj.Position).Magnitude
                if distance < minDist then
                    minDist = distance
                    closestCoin = obj
                end
            end
        end

        if closestCoin then
            farmedCoins[closestCoin] = true
            rootPart.Velocity = Vector3.zero
            rootPart.CFrame = closestCoin.CFrame
            task.wait(0.1)  -- Chờ một chút để coin được nhặt
        else
            -- Hết coin, reset danh sách để quét lại
            farmedCoins = {}
        end
    end
end)

print("✅ Auto Coin Farm đã chạy! (không cần GUI, tự động nhặt coin)")
